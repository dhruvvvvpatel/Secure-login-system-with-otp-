# Secure-login-system-with-otp-
The Secure Login System with OTP Verification is a Python-based cybersecurity project for secure user authentication. Users register with a username and password, which is stored using SHA-256 hashing. During login, the system checks the credentials and sends a 6-digit One-Time Password (OTP) for added security.
